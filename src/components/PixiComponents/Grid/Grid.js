import React from "react";
import { Graphics } from "@pixi/graphics";

const Grid = (props) => {

    const draw = () => {
        
    }

    return (
        <Graphics/>
    )
}

export default Grid;